---
description: BMAD BMM Agent: create-excalidraw-dataflow
globs:
alwaysApply: false
---

IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

<steps CRITICAL="TRUE">
BMAD 6.3+ (Cursor): Do **not** load `workflow.xml` or legacy `_bmad/bmm/workflows/**` paths.

This rule does **not** have a matching skill in a default **core + bmm** install. Use the **bmad-help** skill (`{project-root}/.cursor/skills/bmad-help/`) to find an alternative, or re-run `npx bmad-method install` and add the module that ships this workflow (e.g. Test Architect / diagram bundles). Until then, stop and tell the user which capability is missing instead of inventing a workflow.
</steps>
