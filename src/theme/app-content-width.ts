/** Shared authenticated page content width (Story 9.5 — ~720 tablet / ~960 desktop). */
export const appContentWidthSx = {
  width: "100%",
  maxWidth: { xs: "100%", sm: 720, md: 960 },
  mx: "auto",
} as const;
