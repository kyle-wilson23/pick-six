---
description: BMAD BMM Agent: code-review
globs:
alwaysApply: false
---

IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

<steps CRITICAL="TRUE">
BMAD 6.3+ (Cursor): Do **not** load `workflow.xml`, `workflow.yaml`, or paths under `_bmad/bmm/workflows/` — they are not copied into this repo. Orchestration lives in **Cursor Agent Skills** under `.cursor/skills/`.

1. Read the full `{project-root}/.cursor/skills/bmad-code-review/SKILL.md` (entrypoint).
2. Read the full `{project-root}/.cursor/skills/bmad-code-review/workflow.md` and follow it exactly, including any co-located files that workflow references (templates, checklists, data).
3. When the workflow references `{project-root}/_bmad/bmm/config.yaml` or `{project-root}/_bmad/core/config.yaml`, load those files.
4. Save outputs after EACH section when generating documents from templates.
</steps>
